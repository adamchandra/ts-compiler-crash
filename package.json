{
  "name": "TS Compiler Crash",
  "version": "0.0.1",
  "description": "Demonstrate compiler crash",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "tsc",
    "rebuild": "npm run clean && npm run build"
  },
  "dependencies": {
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/yargs": "~17.0.24",
    "typescript": "^5.1.6"
  }
}
